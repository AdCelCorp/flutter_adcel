# flutter_adcel

Official AdCel SDK plugin for Flutter for Android and iOS.

# Installation

- Add this to your package's pubspec.yaml file:

```yaml
dependencies:
  flutter_adcel: "<LATEST_VERSION>"

```

- Install it - You can install packages from the command line:

```sh
flutter pub get
```

